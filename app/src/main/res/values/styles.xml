<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MoveButtons">
        <item name="android:background">@drawable/button_shape_selected</item>
        <item name="android:tint">@color/colorPrimary</item>
    </style>

    <style name="RightButton" parent="MoveButtons">
        <item name="android:src">@drawable/ic_right</item>
        <item name="android:tint">@color/change_color</item>
    </style>

    <style name="LeftButton" parent="MoveButtons">
        <item name="android:src">@drawable/ic_left</item>
        <item name="android:tint">@color/change_color</item>
    </style>

    <style name="ReturnButton">
        <item name="android:background">@color/colorBackground</item>
        <item name="android:tint">@color/colorPrimary</item>
    </style>

    <style name="RetButton" parent="ReturnButton">
        <item name="android:src">@drawable/ic_return_back</item>
        <item name="android:tint">@color/change_colour_back</item>
    </style>

    <style name="CustomDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!--buttons color-->
        <item name="colorAccent">@color/black</item>
        <!--title and message color-->
        <item name="android:textColorPrimary">@android:color/black</item>
        <!--dialog background-->
        <item name="android:windowBackground">@drawable/my_button_bg</item>
        <item name="android:fontFamily">casual</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>